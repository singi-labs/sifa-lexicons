{
  "name": "@singi-labs/sifa-lexicons",
  "version": "0.1.0",
  "description": "AT Protocol lexicon schemas for Sifa professional profiles (id.sifa.*)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/singi-labs/sifa-lexicons.git"
  },
  "scripts": {
    "validate": "lex gen-api /tmp/sifa-lexicon-validate $(find ./lexicons -name '*.json' ! -name 'authProfileAccess.json' | sort) --yes && rm -rf /tmp/sifa-lexicon-validate",
    "generate-types": "lex gen-api ./src/types $(find ./lexicons -name '*.json' ! -name 'authProfileAccess.json' | sort) --yes"
  },
  "devDependencies": {
    "@atproto/lex-cli": "^0.5.0"
  }
}
